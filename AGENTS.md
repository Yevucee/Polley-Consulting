# Samuel Polley personal website — agent notes

Static HTML/CSS/JS personal site. No build step, no framework.

## Do

- Keep the cinematic dark palette (bone text, Ghana stripe of red / gold / green).
- Put new drone films in `assets/video/` using the slot names in `assets/video/README.md`.
- Preview with `python3 -m http.server 8000`.

## Do not

- Do not turn this into the Polley Consulting marketing site. That lives at polleyconsulting.com.
- Do not add React, Vite, or a bundler unless Samuel asks for it.
- Do not invent extra biography. Copy comes from Samuel’s public consulting site and LinkedIn.

## Cursor Cloud

`.cursor/environment.json` only starts a static server on port 8000. There are no dependencies to install.
